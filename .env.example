# Optional override used by local development. Defaults to http://localhost:8000 locally,
# api-test.curvefever.pro on test hosts, and api.curvefever.pro in production.
VITE_API_ENDPOINT=

# Firebase Admin service-account JSON. In environments with Application Default
# Credentials this can be omitted.
FIREBASE_SERVICE_ACCOUNT_JSON=
